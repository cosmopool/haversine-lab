# Haversine Lab — Computer Enhance, Part 2: Basic Profiling

Profiling harness for the Computer Enhance haversine homework: generate
reproducible coordinate pairs (`generator`), then parse them back and
recompute distances (`processor`) so JSON parsing vs. haversine math can be
profiled separately.
